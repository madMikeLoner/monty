#include "monty.h"

/**
 * notop - has no function
 *  @stack: the stack
 * @line_number: the current line number
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}
